#MONGODB_URI=mongodb+srv://<your_username>:<your_password>@<your_cluster>.mongodb.net/<your_database>?retryWrites=true&w=majority  # For MongoDB Atlas (replace placeholders)
# OR if using local MongoDB:
MONGODB_URI=mongodb://localhost:27017/teacher-parent-platform

PORT=5000  # You can change the port
NODE_ENV=development # or production. If you are deploying, set this to "production"

# Example of other environment variables (if needed)
JWT_SECRET=your_secret_key_here  # For JWT authentication
API_KEY=your_api_key